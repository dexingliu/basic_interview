https://blog.csdn.net/Leon_summer/article/details/82698403
git使用第二步

上传文件：

git init //把这个目录变成Git可以管理的仓库
git add README.md //文件添加到仓库
git add . //不但可以跟单一文件，还可以跟通配符，更可以跟目录。一个点就把当前目录下所有未追踪 的文件全部add了
git commit -m “first commit” //把文件提交到仓库
git remote add origin git@github.com:dexingliu/basic_interview.git //关联远程仓库
上传到github之前先pull一下远程仓库，执行如下命令
git pull origin master
git push -u origin master //把本地库的所有内容推送到远程库上
修改文件：

git add 文件名 //换成你的文件名。
git commit -m “提交index-demo.html”
git push -u origin master
删除文件：

git rm 文件名
git commit -m “删除文件”
git push origin master
